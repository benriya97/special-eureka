#include <stdio.h>

int main()
{
    int n[] = {2,5,9,7,3,1,6,4,0,8};
    int *p, i;
    p = n;
    for(i=0;i<10;i++)
    {
        printf("The integer in location n[%d] in %d \n", i, *(p+i));
    }
    return 0;
}
